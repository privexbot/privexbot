// Runtime Configuration (Development Placeholder)
// In development, this file exists to prevent 404 errors
// In production, this is generated by the Docker entrypoint script

// Check if window.ENV_CONFIG is not already set
if (typeof window !== 'undefined' && !window.ENV_CONFIG) {
  window.ENV_CONFIG = {
    API_BASE_URL: '__API_BASE_URL__',
    WIDGET_CDN_URL: '__WIDGET_CDN_URL__',
    ENVIRONMENT: '__ENVIRONMENT__'
  };
}
