# NPM Configuration for PrivexBot Frontend
# This file configures npm behavior for both local development and Docker builds

# Use legacy peer deps resolution to handle React 19 compatibility
# This allows packages that require React 18 to work with React 19
legacy-peer-deps=true

# Strict SSL verification
strict-ssl=true

# Save exact versions when adding dependencies
save-exact=false
