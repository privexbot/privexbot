# SecretVM Production Deployment with Traefik
# Usage: Upload this file to /mnt/secure/docker_wd/ as docker-compose.yml
# Then: docker compose up -d
#
# Network: privexbot.com (67.43.239.18)
# Subdomains:
#   - api.privexbot.com      (Backend API)
#   - pgadmin.privexbot.com  (PgAdmin)
#   - redis-ui.privexbot.com (Redis Commander)
#   - flower.privexbot.com   (Celery Flower Monitoring)
#   - traefik.privexbot.com  (Traefik Dashboard)
#
# IMPORTANT: Update image digest after building and pushing with:
#   ./scripts/docker/build-push-cpu.sh 0.1.0
#   Then replace sha256:fcc7eb8cadd2cc92697074b5291824c8d2754ef1eb296e1701a689056f7655d8 with the output digest
services:
  backend:
    # REPLACE THIS: Use digest from build-push-cpu.sh output
    image: privexbot/privexbot-backend@sha256:1657fb5b9049786ae76a73d3563e3897e77ca0bef38c94133a7362d7caa87a93
    container_name: privexbot-backend-secretvm
    env_file:
      - usr/.env
    environment:
      # Playwright configuration for production
      - PLAYWRIGHT_BROWSERS_PATH=/home/appuser/.cache/ms-playwright
      - PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    volumes:
      # Persist Playwright browsers for web scraping
      - playwright_browsers:/root/.cache/ms-playwright
    networks:
      - traefik
    # CRITICAL: DNS configuration for reliable email sending
    dns:
      - 8.8.8.8 # Google DNS
      - 8.8.4.4 # Google DNS backup
      - 1.1.1.1 # Cloudflare DNS
    # Ensure container can resolve external hosts
    extra_hosts:
      - "smtp.gmail.com:142.250.80.109" # Gmail SMTP IP (backup)
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - import urllib.request;
          urllib.request.urlopen('http://localhost:8000/health')
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - traefik.enable=true
      - traefik.http.routers.api.rule=Host(`api.privexbot.com`)
      - traefik.http.routers.api.entrypoints=websecure
      - traefik.http.routers.api.tls=true
      - traefik.http.services.api.loadbalancer.server.port=8000
      - traefik.docker.network=traefik

  # Celery worker for background tasks (KB processing, embeddings, crawling)
  celery-worker:
    # REPLACE THIS: Use same digest from build-push-cpu.sh output
    image: privexbot/privexbot-backend@sha256:1657fb5b9049786ae76a73d3563e3897e77ca0bef38c94133a7362d7caa87a93
    container_name: privexbot-celery-secretvm
    command: celery -A app.tasks.celery_worker worker --loglevel=info --queues=default,high_priority,low_priority --concurrency=2
    env_file:
      - usr/.env
    environment:
      # PyTorch/OpenMP compatibility with Celery
      - OMP_NUM_THREADS=1
      - MKL_NUM_THREADS=1
      - OPENBLAS_NUM_THREADS=1
      - VECLIB_MAXIMUM_THREADS=1
      - NUMEXPR_NUM_THREADS=1
      # Playwright configuration for production
      - PLAYWRIGHT_BROWSERS_PATH=/home/appuser/.cache/ms-playwright
      - PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_healthy
    volumes:
      # Persist Playwright browsers for web scraping (Celery tasks need browsers too)
      - playwright_browsers:/root/.cache/ms-playwright
    networks:
      - traefik
    # CRITICAL: DNS configuration for reliable email sending in background tasks
    dns:
      - 8.8.8.8 # Google DNS
      - 8.8.4.4 # Google DNS backup
      - 1.1.1.1 # Cloudflare DNS
    extra_hosts:
      - "smtp.gmail.com:142.250.80.109" # Gmail SMTP IP (backup)
    restart: unless-stopped

  # Celery beat for scheduled tasks (periodic re-indexing, maintenance)
  celery-beat:
    # REPLACE THIS: Use same digest from build-push-cpu.sh output
    image: privexbot/privexbot-backend@sha256:1657fb5b9049786ae76a73d3563e3897e77ca0bef38c94133a7362d7caa87a93
    container_name: privexbot-celery-beat-secretvm
    command: celery -A app.tasks.celery_worker beat --loglevel=info
    env_file:
      - usr/.env
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - traefik
    restart: unless-stopped

  # Flower for Celery monitoring (Web UI at https://flower.harystyles.store)
  flower:
    # REPLACE THIS: Use same digest from build-push-cpu.sh output
    image: privexbot/privexbot-backend@sha256:1657fb5b9049786ae76a73d3563e3897e77ca0bef38c94133a7362d7caa87a93
    container_name: privexbot-flower-secretvm
    command: celery -A app.tasks.celery_worker flower --port=5555 --basic_auth=admin:${FLOWER_PASSWORD}
    env_file:
      - usr/.env
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - traefik
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.routers.flower.rule=Host(`flower.privexbot.com`)
      - traefik.http.routers.flower.entrypoints=websecure
      - traefik.http.routers.flower.tls=true
      - traefik.http.services.flower.loadbalancer.server.port=5555
      - traefik.docker.network=traefik
  qdrant:
    image: qdrant/qdrant:latest
    container_name: privexbot-qdrant-secretvm
    volumes:
      - qdrant_data:/qdrant/storage
    environment:
      - QDRANT__SERVICE__GRPC_PORT=6334
    networks:
      - traefik
    restart: unless-stopped
    healthcheck:
      test:
        ["CMD-SHELL", "timeout 1 bash -c '</dev/tcp/localhost/6333' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    env_file:
      - usr/.env

  postgres:
    image: pgvector/pgvector:pg16
    container_name: privexbot-postgres-secretvm
    environment:
      - POSTGRES_USER=privexbot
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=privexbot
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - traefik
    restart: unless-stopped
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U privexbot -d privexbot
      interval: 10s
      timeout: 5s
      retries: 5
    env_file:
      - usr/.env
  redis:
    image: redis:7-alpine
    container_name: privexbot-redis-secretvm
    volumes:
      - redis_data:/data
    networks:
      - traefik
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 10s
      timeout: 3s
      retries: 5
    env_file:
      - usr/.env
  pgadmin:
    image: dpage/pgadmin4:8.11
    container_name: privexbot-pgadmin-secretvm
    user: root
    environment:
      - PGADMIN_DEFAULT_EMAIL=privexbot@gmail.com
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_PASSWORD}
      - PGADMIN_CONFIG_SERVER_MODE=True
      - PGADMIN_LISTEN_PORT=80
      - PGADMIN_CONFIG_PROXY_X_FOR_COUNT=1
      - PGADMIN_CONFIG_PROXY_X_PROTO_COUNT=1
      - PGADMIN_CONFIG_PROXY_X_HOST_COUNT=1
      - PGADMIN_CONFIG_ENHANCED_COOKIE_PROTECTION=False
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - traefik
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - wget
        - -O
        - "-"
        - http://localhost:80/misc/ping
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    labels:
      - traefik.enable=true
      - traefik.http.routers.pgadmin.rule=Host(`pgadmin.privexbot.com`)
      - traefik.http.routers.pgadmin.entrypoints=websecure
      - traefik.http.routers.pgadmin.tls=true
      - traefik.http.services.pgadmin.loadbalancer.server.port=80
      - traefik.docker.network=traefik
    env_file:
      - usr/.env
  redis-ui:
    image: rediscommander/redis-commander:latest
    container_name: privexbot-redis-ui-secretvm
    environment:
      - REDIS_HOSTS=local:redis:6379
    env_file:
      - usr/.env
    depends_on:
      - redis
    networks:
      - traefik
    restart: unless-stopped
    labels:
      - traefik.enable=true
      - traefik.http.routers.redisui.rule=Host(`redis-ui.privexbot.com`)
      - traefik.http.routers.redisui.entrypoints=websecure
      - traefik.http.routers.redisui.tls=true
      - traefik.http.services.redisui.loadbalancer.server.port=8081
      - traefik.docker.network=traefik
  traefik:
    image: traefik:v2.10
    container_name: traefik-secretvm
    command:
      - --api=true
      - --api.dashboard=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --entrypoints.websecure.http.tls.options=default@file
      - --providers.file.directory=/etc/traefik/dynamic
      - --providers.file.watch=true
    ports:
      - 80:80
      - 443:443
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /mnt/secure/cert:/certs:ro
    networks:
      - traefik
    restart: unless-stopped
    configs:
      - source: tls_config
        target: /etc/traefik/dynamic/tls.yml
    labels:
      - traefik.enable=true
      - traefik.http.routers.traefik.rule=Host(`traefik.privexbot.com`)
      - traefik.http.routers.traefik.entrypoints=websecure
      - traefik.http.routers.traefik.tls=true
      - traefik.http.routers.traefik.service=api@internal
      - traefik.docker.network=traefik
    env_file:
      - usr/.env
volumes:
  postgres_data: null
  redis_data: null
  pgadmin_data: null
  qdrant_data: null
  playwright_browsers: null # Persistent browser cache
networks:
  traefik:
    driver: bridge
configs:
  tls_config:
    content: |-
      tls:
        certificates:
          - certFile: /certs/secret_vm_fullchain.pem
            keyFile: /certs/secret_vm_private.pem
        stores:
          default:
            defaultCertificate:
              certFile: /certs/secret_vm_fullchain.pem
              keyFile: /certs/secret_vm_private.pem
